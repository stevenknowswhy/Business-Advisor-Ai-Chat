// Convex server-side action (example)
// export default function createConversation(ctx, { ownerId, title, advisorIds }) {
//   const { db } = ctx;
//   return db.table('conversations').insert({ ownerId, title, advisorIds, createdAt: new Date().toISOString() });
// }

// Adjust to the server SDK API shape for your Convex version.
